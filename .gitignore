# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Editor
.vscode/
*.swp
*.swo

# PCK runtime — local state changes per run, not committed
# (keep initial genesis state for demo; ignore subsequent mutations)
# To track state: remove these lines
pck_orchestrator/state/ledger.jsonl
pck_orchestrator/events/inbox.jsonl

# Outbox proofs — local witness artifacts
# (tracked here as demo; production outbox would push separately)
# pck_orchestrator/outbox/
